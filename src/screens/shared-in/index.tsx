import React from 'react'
import { SharedIn } from '@root/components'
import { SafeAreaView } from 'react-native'

const SharedInScreen = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <SharedIn />
    </SafeAreaView>
  )
}

export default SharedInScreen
